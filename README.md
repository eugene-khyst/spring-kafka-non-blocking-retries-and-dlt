This sample demonstrates how to use Kafka transactions in Spring Boot applications and synchronize them with database transactions.

Run this sample using

```
./gradlew clean test -i -Dkafka.embedded.port=9092
```